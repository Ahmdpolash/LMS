import React from 'react';
import CustomLoading from './_components/CustomLoading';

const Loading = () => {
    return (
        <div>
            <CustomLoading/>
        </div>
    );
};

export default Loading;